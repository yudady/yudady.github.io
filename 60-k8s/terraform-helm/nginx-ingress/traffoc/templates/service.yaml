apiVersion: v1
kind: Service
metadata:
  name: {{ include "traffoc.fullname" . }}
  labels:
    {{- include "traffoc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "traffoc.selectorLabels" . | nindent 4 }}
